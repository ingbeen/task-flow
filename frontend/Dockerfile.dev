FROM node:22-alpine

WORKDIR /app

# 의존성 사전 설치 (bind mount 시 anonymous volume으로 보존)
COPY package.json package-lock.json ./
RUN npm ci

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
