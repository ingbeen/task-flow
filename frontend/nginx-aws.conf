server {
    listen 80;
    server_name _;

    # React 정적 파일 + SPA fallback (ALB가 API 라우팅 담당)
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
